<h2><%= gettext "New user" %></h2>

<%= render "form.html", changeset: @changeset, action: user_path(@conn, :create, @locale) %>

<span><%= link gettext("Back"), to: user_path(@conn, :index, @locale) %></span>
